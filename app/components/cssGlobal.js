import colors from "../scss/colors";
import functions from "../scss/styleFunctions";

let cssGlobalFunction = {
	onePager: function(arabicToggle) {
		const arabic = ``;
		return `
		

		` + (arabicToggle ? arabic : "");
	}
};

export default cssGlobalFunction;